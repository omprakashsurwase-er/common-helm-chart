{{- if .Values.secret.enabled }}

apiVersion: v1
kind: Secret

metadata:
  name: {{ include "common-helm-chart.fullname" . }}

type: Opaque

stringData:
{{ toYaml .Values.secret.data | indent 2 }}

{{- end }}
